//
//  BarButtonController.swift
//  Midterm
//
//  Created by Linsey Briley on 3/28/19.
//  Copyright © 2019 AppCoda. All rights reserved.
//

import UIKit

class BarButtonController: UIBarButtonItem {

    func backButtonBackgroundImage(for: UIControl.State, barMetrics: UIBarMetrics) -> UIImage?{
        
    }
    //Returns the back button background image for a given control state and bar metrics.
    
    func setBackButtonBackgroundImage(UIImage?, for: UIControl.State, barMetrics: UIBarMetrics){
        
    }
    //Sets the back button background image for a given control state and bar metrics
    
    func backButtonTitlePositionAdjustment(for: UIBarMetrics) -> UIOffset{
        
    }
    //eturns the back button title offset for given bar metrics.
    
    func setBackButtonTitlePositionAdjustment(UIOffset, for: UIBarMetrics){
        
    }
    //Sets the back button title offset for given bar metrics
    
    override func backButtonBackgroundVerticalPositionAdjustment(for: UIBarMetrics) -> CGFloat{
        
    }
    //Returns the back button vertical position offset for given bar metrics.
    
    func setBackButtonBackgroundVerticalPositionAdjustment(_: CGFloat, for: UIBarMetrics){
        
    }
    //Sets the back button vertical position offset for given bar metrics.
    
    override func backgroundVerticalPositionAdjustment(for: UIBarMetrics) -> CGFloat{
        
    }
    //Returns the background vertical position offset for given bar metrics.
    
    func setBackgroundVerticalPositionAdjustment(_: CGFloat, for: UIBarMetrics){
        
    }
    //Sets the background vertical position offset for given bar metrics.
    
    override func backgroundImage(for: UIControl.State, barMetrics: UIBarMetrics) -> UIImage?{
        
    }
    //Returns the background image for a given state and bar metrics.
    
    func setBackgroundImage(_: UIImage?, for: UIControl.State, barMetrics: UIBarMetrics){
        
    }
    //Sets the background image for a given state and bar metrics.
    
    override func backgroundImage(for: UIControl.State, style: UIBarButtonItem.Style, barMetrics: UIBarMetrics) -> UIImage?{
        
    }
    //Returns the background image for the specified state, style, and metrics.
    
    override func setBackgroundImage(_: UIImage?, for: UIControl.State, style: UIBarButtonItem.Style, barMetrics: UIBarMetrics){
        
    }
    //Sets the background image for the specified state, style, and metrics.
    
    override func titlePositionAdjustment(for: UIBarMetrics) -> UIOffset{
        
    }
    //Returns the title offset for given bar metrics.
    
    override func setTitlePositionAdjustment(_: UIOffset, for: UIBarMetrics){
        
    }
    //Sets the title offset for given bar metrics.
    
}
